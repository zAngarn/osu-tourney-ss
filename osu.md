# Tener en cuenta: 
# - lt = 1 -> P(X<=x), lt = 0 -> P(X>x) 
# - qnorm, qt, pnorm [lt=0 para p-valor]
# - var.test, t.test, prop.test
# - Fórmulas:
#     -> T  = ((X - mu) * sqrt(n))/S
#     -> IC = qt() * S/sqrt(n)
# - Distr:
#     -> E(U) = (a + b) * 1/2
#     -> Var  = (b - a)^2 * 1/12